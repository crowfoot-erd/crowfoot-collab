spring:
  application:
    name: crowfoot-collab

server:
  port: 8083
  shutdown: graceful   # 무중단 롤아웃 — SIGTERM 후 처리 중 요청을 마무리하고 종료 (k8s RollingUpdate)

management:
  endpoints:
    web:
      exposure:
        include: health
